<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@layout/background"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/selectRecipeLabel"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/selectRecipes" 
        android:textColor="@color/white"
        android:layout_below="@+id/Welcome"/>

    <Button
        android:id="@+id/btnFilterRecipes"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/filter" 
        android:onClick="PopulateSpinner"
        android:layout_below="@+id/selectRecipeLabel"
        android:layout_alignTop="@+id/filterEditText"
        android:layout_alignBottom="@+id/filterEditText"
        android:layout_alignParentRight="true" />

    <EditText
        android:id="@+id/filterEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/selectRecipeLabel"
        android:layout_toLeftOf="@+id/btnFilterRecipes"
        android:ems="10"
        android:hint="@string/filterHint" >

        <requestFocus />
    </EditText>

    <ListView
        android:id="@+id/recipeListView"
        android:layout_width="match_parent"
        android:layout_height="137dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/filterEditText"
        android:layout_marginTop="13dp"
        android:choiceMode="singleChoice"
        android:listSelector="@color/white" >

    </ListView>

    <TextView
        android:id="@+id/Welcome"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:gravity="center"
        android:text="@string/welcome"
        android:textColor="@color/white"
        android:textSize="40sp" />
   

</RelativeLayout>